# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml

# Python (services/extractor)
__pycache__/
*.pyc
.pytest_cache/
services/extractor/.venv/

# Android
android/.gradle/
android/local.properties
android/key.properties
*.keystore
*.jks

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Secrets / local config
*.env
.env*
secrets.dart

# ML models (large; document source in CLAUDE.md instead)
*.tflite

# Giphy API key — a real credential. Kept as a local file for convenience and
# passed to the build with --dart-define; it must never enter a commit.
giphy_api_key.txt
