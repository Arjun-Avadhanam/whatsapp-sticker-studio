# Runtime
fastapi>=0.140
uvicorn>=0.30
pydantic>=2.0
# yt-dlp intentionally a floor, not a pin: it must stay updatable, since a
# Twitter-format change is fixed by upgrading it. Pin to the tested version only
# once real extraction has been verified against a live tweet.
yt-dlp>=2026.7.4

# Test-only (used by CI's pytest job)
pytest>=8.0
httpx>=0.27
